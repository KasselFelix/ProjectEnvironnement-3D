# Sortie de compilation
bin/

# Sortie de compilation des tests et fat-jar JUnit (téléchargé via test-setup.sh)
test/bin/
test/lib/

# Crash dumps JVM
hs_err_pid*.log
core.*
replay_pid*.log

# Eclipse (les laisser dehors si tu veux que d'autres importent en Eclipse)
.settings/

# IDE locaux — on commit les configs partagées (tasks/launch/extensions),
# on ignore le reste (settings utilisateur, cache, etc.)
.vscode/*
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
!.vscode/settings.json

.idea/
*.iml

# macOS
.DS_Store

# Linux
*~

# Backups éventuels
*.bak
JOGL-*-backup/

# Captures d'écran générées en jeu (F12)
screenshots/

